# Dependencies — reinstalled inside Docker
node_modules
npm-debug.log*

# Next.js build output — rebuilt inside Docker
.next
out

# Local env files — secrets are injected via Render env vars, never baked into image
.env
.env.*
!.env.example

# Git + editor
.git
.gitignore
.github
.vscode

# Docker files themselves
Dockerfile
.dockerignore
docker-compose.yml

# Express backend — not needed in the frontend image
server

# Misc
.DS_Store
*.pem
coverage
README.md
